title: My Minimal Site
description: A small Jekyll site using jekyll-theme-minimal
remote_theme: pages-themes/minimal@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one
# For user sites set url to https://<your-username>.github.io
# url: "https://USERNAME.github.io"
# baseurl: "" # leave blank unless deploying to a subpath
